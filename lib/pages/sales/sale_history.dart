import 'package:flutter/material.dart';

class SaleHistory extends StatefulWidget {
  @override
  _SaleHistory createState() => _SaleHistory();
}

class _SaleHistory extends State<SaleHistory> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: ListView(),
    );
  }
}
